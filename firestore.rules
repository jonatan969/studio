rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This single rule matches any document in any collection or subcollection.
    // It allows any authenticated user to perform any read or write operation.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
